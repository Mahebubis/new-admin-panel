<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- The post-call popup. SYSTEM_ALERT_WINDOW is what lets it appear over the dialer or the
         launcher the moment a call ends; POST_NOTIFICATIONS covers the notification fallback. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lets the post-call card open by itself when the call ends on a locked phone, the same way
         a dialer shows an incoming call. Without the overlay permission this is the only route
         Android leaves open. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Runs the carrier's own balance code (*121# and the like) in the background, per SIM, to
         read a plan's validity. Android exposes no other way to learn it. -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Keeps CallIQ running between calls (CallMonitorService). Without it Android 14+ freezes
         the app and Xiaomi/Oppo/Samsung kill it, so live calls stop reaching the panel.
         "specialUse" because no standard type fits: this app is not the phone's dialer. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Restart tracking after the phone reboots or CallIQ updates. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <queries>
        <!-- Package visibility declarations for OEM/ColorOS system framework services -->
        <package android:name="com.oplus.aiseal" />
        <package android:name="com.oplus.aiunit" />
        <package android:name="com.oplus.strategycenter" />
        <package android:name="com.coloros.phonemanager" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The post-call card as a screen: opened by tapping the notification, and thrown up by
            the notification's full-screen intent when the phone is locked. Kept out of Recents and
            given no history so it never lingers behind the counselor's real work.
        -->
        <activity
            android:name=".CallPopupActivity"
            android:exported="false"
            android:theme="@style/Theme.Calliq.Popup"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleTask"
            android:taskAffinity=".popup"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode" />

        <receiver
            android:name=".CallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".CallMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Tracks the counselor's call states (ringing, on call, ended) so a company CRM can show live calls and log every call." />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Outcome buttons on the post-call notification. Not exported: only our own
             PendingIntents fire it. -->
        <receiver
            android:name=".OutcomeActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.calliq.TAG_OUTCOME" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
